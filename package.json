{
  "name": "ethereum-blocks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ blocklytics/ethereum-blocks",
    "create:neon": "graph create sobal/sobal-neon-blocks --node https://neon-subgraph.sobal.fi/deploy/",
    "remove:neon": "graph remove sobal/sobal-neon-blocks --node https://neon-subgraph.sobal.fi/deploy/",
    "deploy:base": "graph deploy --studio sobal-base-blocks",
    "deploy:neon": "graph deploy sobal/sobal-neon-blocks --ipfs https://neon-subgraph.sobal.fi/ --node https://neon-subgraph.sobal.fi/deploy/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.54.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}
